export default class AcaiItem {
  constructor (description, time, cost, image, id) {
    this.description = description
    this.time = time
    this.cost = cost
    this.image = image
    this.id = id
  }
}
